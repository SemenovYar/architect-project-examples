import React, { memo } from 'react';

import styles from './styles.module.scss'

type Props = {
}

const {{toCamelCase(componentName)}}: React.FC<Props> = ({}) => {

  return true;
}

export default memo({{toCamelCase(componentName)}})

const eslintTest = 'sdf'
